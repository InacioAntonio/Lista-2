// Preciso calcular uma raiz quadrada ou seja calcular o delta e preciso usar raiz vo precisa da variavel delta de cria\

/*#include <iostream>
#include <cmath>
using namespace std;

int main(){
  float raiz1,raiz2,a,b,c,x,delta;
  cout << "\nDigite o valor de a";
  cin >> a;
  cout << "\nDigite o valor de b";
  cin >> b;
  cout << "\nDigite o valor de c";
  cin >> c;
  if(a==0){
    cout << "Invalido\n";
  } else{
     delta=pow(b,2)-4*a*c;
    if (delta<0){
       cout << "Não existe raiz real\n";
    } else{
      if(delta==0){
        cout << "Raiz única\n";
        raiz1 = -b /(2*a);
          cout << "\nA unica raiz é:"<<raiz1;
          } else {
           }if (delta>0) {
          cout << "Existem duas raizes possiveis\n";
          raiz1=(-b+sqrt(delta))/2*a;
            raiz2=(-b-sqrt(delta))/2*a;
          cout << "\nO valor da primeira raiz é de: "<<raiz1<<"\ne da segunda raiz caso  haja é de: "<<raiz2;
        }
      }
      
      
        
                
          
        
        
      }
     
    
  

  
   
  
   
  
  
    

    
    
  
}*/